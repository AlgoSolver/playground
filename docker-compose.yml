version: '3.9'
services:
  web:
    build:
      context: ./
    volumes:
      - .:/src
    command: npm start
    ports:
      - "1234:3000"
    environment:
      NODE_ENV: development
      DEBUG: algosolver-playground:*